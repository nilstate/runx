skill: post-merge-final-outcome-publisher
version: "0.1.0"
runners:
  default:
    default: true
    type: thread-outbox-provider
    thread_outbox_provider:
      operation: push
      manifest_path: manifest.json
      push_path: push.json
