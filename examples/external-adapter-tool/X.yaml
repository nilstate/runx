skill: external-adapter-echo
version: "0.1.0"
runners:
  default:
    default: true
    type: external-adapter
    external_adapter:
      manifest_path: manifest.json
    inputs:
      message:
        type: string
        required: false
        description: Optional message echoed back through the adapter.
