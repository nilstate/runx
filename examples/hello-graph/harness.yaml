name: hello-graph
kind: graph
target: ./graph.yaml
expect:
  status: sealed
  steps:
    - first
    - second
  receipt:
    schema: runx.receipt.v1
    harness_id: hrn_hello-graph_graph
    state: sealed
    disposition: closed
    reason_code: graph_closed
    child_receipt_count: 2
