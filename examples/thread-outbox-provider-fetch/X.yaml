skill: thread-outbox-provider-fetch
version: "0.1.0"
runners:
  default:
    default: true
    type: thread-outbox-provider
    thread_outbox_provider:
      operation: fetch
      manifest_path: manifest.json
      fetch_path: fetch.json
