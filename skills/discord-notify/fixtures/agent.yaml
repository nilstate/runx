name: echo-agent-replay
lane: agent
target:
  kind: skill
  ref: .
inputs:
  message: hello
agent:
  mode: replay
expect:
  status: sealed
  outputs:
    echo_packet:
      matches_packet: discord.notify.echo.v1
