name: spend-stripe-spt-operation-boundary
kind: graph
target: ../harness/stripe-spt-operation.yaml
expect:
  status: sealed
  steps:
    - quote
    - reserve
    - fulfill
  step_outputs:
    fulfill:
      subset:
        effect_evidence_packet:
          data:
            rail_result:
              status: fulfilled
              rail: stripe-spt
              amount_minor: 125
              currency: USD
              counterparty: merchant:demo
            settlement_proof:
              payment_admission_id: sha256:payment-admission-operation-001
              money_movement_id: sha256:money-movement-operation-001
              proof_status: fulfilled
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: spend
  source_case: stripe-spt-operation-boundary
  source: skills-fixture
