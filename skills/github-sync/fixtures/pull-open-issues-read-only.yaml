name: github-sync-plans-bounded-pull
kind: skill
target: ..
runner: github-sync
inputs:
  repo: runxhq/runx
  direction: pull
  resources:
    kind: issues
    filters: { state: open, labels: [triage], limit: 25 }
  scope: read
expect:
  status: sealed
  output:
    subset:
      sync_plan:
        decision: ready
        scope_used: repo.read
        resources_touched: []
        provider_status: not_called
        gates: { approval_required: false, approval_ref: "" }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: github-sync, source_case: bounded-pull-plan, source: skills-fixture }
