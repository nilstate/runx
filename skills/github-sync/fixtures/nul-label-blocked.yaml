name: github-sync-blocks-nul-label
kind: skill
target: ..
runner: github-sync
inputs:
  repo: runxhq/runx
  direction: push
  resources:
    kind: issues
    filters: { limit: 1 }
    mutations:
      - ref: issues/241
        op: update
        payload:
          labels: ["bug\0hidden"]
  scope: write
expect:
  status: failure
metadata: { public_skill: github-sync, source_case: nul-label, source: skills-fixture }
