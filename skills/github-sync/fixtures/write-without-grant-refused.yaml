name: write-without-grant-refused
kind: skill
target: ..
runner: github-sync
inputs:
  repo: runxhq/runx
  direction: push
  scope: write
  resources:
    kind: issues
    filters:
      refs:
        - runxhq/runx#412
caller:
  answers:
    agent_task.github-sync.output:
      sync_plan:
        decision: refused
        repo: runxhq/runx
        direction: push
        resources_touched:
          - kind: issue
            ref: runxhq/runx#412
        diff_summary: []
        scope_used: repo:read
        gates:
          approval_required: true
        blockers:
          - write requested without a repo:write grant
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: github-sync
  source_case: write-without-grant-refused
  source: skills-fixture
