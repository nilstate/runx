name: release-exact-command-readback
kind: graph
target: ../harness/release-fixture.graph.yaml
operator_journeys:
  - mode: standalone
    exercises_runner: release
    confusors: [issue-to-pr, sourcey]
    request: Release version 1.2.3 through this project-owned exact command profile and verify it.
    expected_outcome: Prepare notes, obtain exact approval, execute once, and return independent release readback.
  - mode: composed
    request: Publish the already prepared 1.2.3 release and verify finality.
    expected_outcome: Reuse the supplied release notes and project command profile without rebuilding or retesting the release payload.
    prior_evidence:
      - The prepared release-note packet and exact project command profile used by the fixture graph.
    must_not_repeat:
      - Do not regenerate approved release notes.
      - Do not execute the publish command after readback proves the version exists.
caller:
  answers:
    agent_task.release-notes.output:
      release_notes:
        headline: Fixture 1.2.3
        summary: Publishes the fixture through the project-owned command profile.
        changelog:
          added: []
          fixed: [Prove independent release readback.]
          changed: []
          removed: []
          breaking: []
        upgrade_guidance: No migration is required.
        risks: []
  approvals:
    release.prepare.approval: true
    release.publish.approval: true
expect:
  status: sealed
  receipt: { schema: runx.receipt.v1 }
  steps: [setup, release]
  step_outputs:
    release:
      subset:
        verification:
          data:
            phase: verify
            status: verified
            version: 1.2.3
            channel: fixture
metadata:
  public_skill: release
  source_case: exact-command-readback
  source: skills-fixture
