{
  "$id": "https://schemas.example.invalid/invoice/v1",
  "type": "object",
  "required": ["id", "status"],
  "properties": {
    "id": { "type": "string" },
    "status": { "type": "string", "enum": ["draft", "paid"] }
  }
}
