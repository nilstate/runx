name: malformed-receipt-ref-stops
kind: skill
target: ..
runner: bundle
inputs:
  receipt_refs:
    - not-a-runx-receipt
  verification_json:
    receipt_ref: not-a-runx-receipt
    verdict: pass
  artifact_links:
    - kind: evidence
      url: https://example.com/evidence.json
      public: true
  reviewer_context: Merge review with malformed receipt evidence.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
    state: sealed
    disposition: closed
metadata:
  public_skill: receipt-evidence-bundle
  source_case: malformed-receipt-ref-stops
  source: skills-fixture
