name: google-analytics-default-requires-provider-binding
kind: skill
target: ..
operator_journeys:
  - mode: refusal
    confusors: [google-search-console, organic-growth]
    request: Show which organic landing pages produced engaged sessions and conversions without configuring an analytics account.
    expected_outcome: Refuse immediately with one provider-binding message before claiming analytics evidence.
inputs:
  property: properties/123456
  date_ranges: [{ start_date: 30daysAgo, end_date: yesterday }]
  dimensions: [landingPagePlusQueryString]
  metrics: [sessions, keyEvents]
expect:
  status: policy_denied
metadata: { public_skill: google-analytics, source_case: direct-default-preflight, source: skills-fixture }
