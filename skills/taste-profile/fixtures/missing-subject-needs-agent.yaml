name: taste-profile-thin-evidence-seals-stop
kind: skill
target: ..
runner: profile
inputs:
  subject: runx maintainer
  evidence:
    - kind: instruction
      content: Keep it clean.
expect:
  status: sealed
  steps: [index, digest, reject]
  step_outputs:
    reject:
      subset:
        taste_profile_packet:
          data:
            decision: needs_more_evidence
            validation: { status: fail }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: taste-profile, source_case: thin-evidence-stop, source: skills-fixture }
