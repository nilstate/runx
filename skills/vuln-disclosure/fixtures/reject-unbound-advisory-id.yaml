name: reject-unbound-advisory-id
kind: skill
target: ..
runner: vuln-disclosure
inputs:
  channel: security_page
  target:
    locator: https://status.example.test/security
  advisory_packet:
    schema: runx.security.vulnerability_advisory.v1
    decision: ready_for_review
    advisory_draft:
      title: Example advisory
      body: Verified advisory body.
      affected_advisory_ids:
        - GHSA-example-1234
    evidence_binding:
      receipt_ref: runx:receipt:sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
      evidence_digest: sha256:bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
    validation:
      status: pass
      findings: []
    publication_status: not_published
caller:
  answers:
    agent_task.vuln-disclosure-review.output:
      disclosure_review_draft:
        decision: ready_for_approval
        rationale: Review attempted to widen the source.
        affected_advisory_ids:
          - CVE-invented
        checklist: []
expect:
  status: sealed
  steps: [admit, digest-payload, review, finalize]
  step_outputs:
    finalize:
      subset:
        disclosure_packet:
          data:
            decision: needs_more_evidence
            validation:
              status: fail
            publication_status: not_published
            provider_status: not_called
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: vuln-disclosure
  source_case: reject-unbound-advisory-id
  source: skills-fixture
