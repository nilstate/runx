{
  "schema": "runx.tool.manifest.v1",
  "name": "data.redis",
  "version": "0.3.0",
  "description": "Redis event-store adapter for the provider-agnostic runx data operation envelope.",
  "source": {
    "type": "cli-tool",
    "command": "node",
    "args": ["./run.mjs"]
  },
  "inputs": {
    "operation": {
      "type": "string",
      "required": true,
      "description": "append_event, read_events, read_projection, or list_stream_heads."
    },
    "data_source_binding": {
      "type": "json",
      "required": true,
      "description": "Non-secret data-source binding injected by the native data-operation router."
    }
  },
  "scopes": ["runx:data:read", "runx:data:append"]
}
