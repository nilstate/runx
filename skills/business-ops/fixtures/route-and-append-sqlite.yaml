name: route-persists-with-provider-evidence
kind: skill
target: ..
runner: route_and_append
env:
  RUNX_DATA_SOURCES: '{"data_sources":{"tenant://business-ops/sqlite-routes":{"adapter":"data.sqlite","database_path":".runx/data/business-ops-routes.sqlite","resources":{"ops_routes":{"kind":"event_stream","partition_key":"aggregate_id"}}}}}'
inputs:
  data_source_ref: tenant://business-ops/sqlite-routes
  resource: ops_routes
  aggregate_id: launch-readiness
  expected_version: 0
  idempotency_key: launch-readiness:classify:v1
  signal: Launch readiness for API v2 with docs, release, customer comms, and spend checks.
  operator_context: Live sends route through send-as; payment movement requires a spend gate and provider readback.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
    state: sealed
    disposition: closed
    reason_code: graph_closed
    child_receipt_count: 3
  steps:
    - classify
    - persist-route
    - readback
  step_outputs:
    persist-route:
      subset:
        data_operation_result:
          data:
            schema: runx.data.operation_result.v1
            operation: append_event
            aggregate_id: launch-readiness
            after_version: 1
            provider_evidence:
              adapter: data.sqlite
              provider: sqlite-event-store
              storage_class: sqlite
    readback:
      subset:
        data_operation_result:
          data:
            schema: runx.data.operation_result.v1
            operation: read_projection
            status: read
            projection:
              aggregate_id: launch-readiness
              event_count: 1
              version: 1
            provider_evidence:
              adapter: data.sqlite
              provider: sqlite-event-store
              storage_class: sqlite
metadata:
  public_skill: business-ops
  source_case: route-persists-with-provider-evidence
  source: skills-fixture
  runner_kind: graph
  graph_shape: route_and_append
