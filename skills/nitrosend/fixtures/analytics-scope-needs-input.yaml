name: analytics-scope-needs-input
kind: skill
target: ..
runner: analytics
inputs:
  objective: Show me the numbers.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: nitrosend
  source_case: analytics-scope-needs-input
  source: skills-fixture
