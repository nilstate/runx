name: list-actions
kind: skill
target: ..
runner: list_actions
inputs:
  data_source_ref: local://runx/operator-inbox/list-actions
  event_types:
    - operator_inbox.action.open
  limit: 10
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
  steps:
    - read
metadata:
  public_skill: operator-inbox
  source_case: list-actions
  source: skills-fixture
  runner_kind: graph
