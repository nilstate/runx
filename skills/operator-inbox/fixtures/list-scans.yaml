name: list-scans
kind: skill
target: ..
runner: list_scans
inputs:
  data_source_ref: local://runx/operator-inbox/list-scans
  limit: 1
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
  steps:
    - read
metadata:
  public_skill: operator-inbox
  source_case: list-scans
  source: skills-fixture
  runner_kind: graph
