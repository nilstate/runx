name: brand-voice-missing-brand-seals-stop
kind: skill
target: ..
runner: voice
inputs:
  source_material:
    - kind: approved
      content: Proof-first governed operations.
expect:
  status: sealed
  steps: [index, digest, reject]
  step_outputs:
    reject:
      subset:
        brand_voice_packet:
          data:
            decision: needs_more_evidence
            brand: ""
            validation: { status: fail }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: brand-voice, source_case: missing-brand-stop, source: skills-fixture }
