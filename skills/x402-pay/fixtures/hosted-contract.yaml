name: x402-pay-hosted-contract
kind: skill
target: ..
runner: pay
operator_journeys:
  - { mode: standalone, confusors: [spend, stripe-pay], request: Pay this exact x402 challenge., expected_outcome: Require hosted x402 authority and return provider readback. }
  - mode: composed
    request: Continue this admitted x402 payment.
    expected_outcome: Reuse supplied challenge and authority evidence.
    prior_evidence: [The admitted x402 challenge supplied by the caller.]
    must_not_repeat: [Do not mint a second x402 payment identity.]
inputs:
  payment_signal: { challenge_id: ch_test_001, amount_minor: 125, currency: USD, counterparty: "merchant:demo" }
  parent_payment_authority: { term_id: "authority:test" }
  idempotency_seed: x402-test-001
caller:
  approvals: { "provider-effect:sha256:3f1d7b31b2a81870701ada2afe08d5f7baafefe2c18feadd4700b96cc743e5ab": true }
  provider_responses:
    principal_id: x402-fixture
    grants: [{ grant_id: x402-fixture, provider: x402, scopes: [payment.x402, payment.x402.read] }]
    operations:
      - { grant_id: x402-fixture, operation: payment.x402, target: "merchant:demo", access: mutate, result: { payment_ref: payment_x402_001, challenge_id: ch_test_001, amount_minor: 125, currency: USD, status: settled } }
      - { grant_id: x402-fixture, operation: payment.x402.read, target: "merchant:demo", access: read, result: { payment_ref: payment_x402_001, challenge_id: ch_test_001, amount_minor: 125, currency: USD, status: settled } }
expect: { status: sealed, receipt: { schema: runx.receipt.v1 } }
