name: x402-pay-default-pays-and-verifies
kind: skill
target: ..
operator_journeys:
  - mode: standalone
    confusors: [spend, stripe-pay]
    request: Pay this exact x402 challenge under the supplied bounded payment authority.
    expected_outcome: Validate the quote once, pay once after approval, and return provider readback.
  - mode: composed
    request: Continue payment from this admitted x402 challenge and parent authority.
    expected_outcome: Reuse the challenge, authority, and retry identity without re-admission, requoting, or duplicate payment.
    prior_evidence:
      - The admitted x402 payment signal and bounded parent payment authority supplied in the fixture.
    must_not_repeat:
      - Do not revalidate or requote the challenge.
      - Do not pay again after settlement evidence exists.
inputs:
  payment_signal:
    signal_type: effect_required
    challenge_id: ch_x402_001
    amount_minor: 125
    currency: USD
    rail: x402
    counterparty: merchant:demo
    operation: search.paid
    realm: test
  parent_payment_authority:
    term_id: authority-term:payment:x402-demo
    principal_ref: { type: host, uri: principal:operator:test }
    resource_ref: { type: surface, uri: merchant:demo }
    resource_family: effect
    verbs: [prepare, commit]
    bounds:
      effect_limits:
        - family: payment
          unit: USD
          max_per_call_units: 125
          max_per_run_units: 125
          channels: [x402]
          realm: test
          peer: merchant:demo
          operation: search.paid
          idempotency_required: true
          recovery_required: true
          receipt_before_success: true
          single_use_capability: true
          authorization_form: single_use_capability
    conditions: []
    approvals: []
    capabilities: [effect_single_use_capability]
    issued_by_ref: { type: host, uri: authority:payment:test }
  realm: test
  idempotency_seed: demo-x402-001
caller:
  provider_responses:
    principal_id: x402-fixture
    grants:
      - grant_id: x402-fixture
        provider: x402
        scopes: [payment.x402, payment.x402.read]
    operations:
      - grant_id: x402-fixture
        operation: payment.x402
        target: merchant:demo
        access: mutate
        result: { payment_ref: pay_x402_001, challenge_id: ch_x402_001, amount_minor: 125, currency: USD, status: settled }
      - grant_id: x402-fixture
        operation: payment.x402.read
        target: merchant:demo
        access: read
        result: { payment_ref: pay_x402_001, challenge_id: ch_x402_001, amount_minor: 125, currency: USD, status: settled }
  approvals:
    "provider-effect:sha256:221124262ed3baf7be19464212564bd8b6046309d120c16b131f586c6b14dd86": true
expect:
  status: sealed
  steps: [plan, pay, readback]
  receipt: { schema: runx.receipt.v1, state: sealed, disposition: closed }
metadata: { public_skill: x402-pay, source_case: direct-default-success, source: skills-fixture }
