name: agent-handoff-needs-contract
kind: skill
target: ..
runner: handoff
inputs:
  task: Improve auth.
caller:
  answers:
    agent_task.agent-handoff.output:
      handoff_packet:
        status: needs_agent
        task:
          deliverable: Improve auth.
          boundary: ""
        to_agent: ""
        grants: []
        success_criteria: []
        deadline:
          value: ""
          source: derived
        context_refs: []
        gates:
          approval_required: false
          approval_ref: null
        acceptance:
          checklist: []
        blockers:
          - task is an unbounded role, not a bounded deliverable
          - no receiver named
          - no grants provided
          - no success criteria provided
expect:
  status: needs_agent
metadata:
  public_skill: agent-handoff
  source_case: agent-handoff-needs-contract
  source: skills-fixture
