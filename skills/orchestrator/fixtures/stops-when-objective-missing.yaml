name: stops-when-objective-missing
kind: skill
target: ..
runner: orchestrator
inputs:
  available_skills:
    - ref: refund@0.1.0
      purpose: Govern one refund linked to a sealed original charge receipt.
  constraints:
    budget_usd: 50
caller:
  answers:
    agent_task.orchestrator.output:
      run_plan:
        objective: ""
        steps: []
        topology: []
        budget:
          ceiling_usd: 50
          allocations: {}
        blockers:
          - objective is missing
        status: needs_agent
expect:
  status: needs_agent
metadata:
  public_skill: orchestrator
  source_case: stops-when-objective-missing
  source: skills-fixture
