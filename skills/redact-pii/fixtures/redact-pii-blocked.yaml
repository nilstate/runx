name: redact-pii-blocked
kind: skill
target: ..
runner: redact-pii
inputs:
  content: "Patient Avery Chen, born 1987-04-12, lives at 14 King Street."
  mode: block
  locale: en-US
caller:
  answers:
    agent_task.redact-pii-detect.output:
      redaction_draft:
        decision: blocked
        detected: []
        residual_risk:
          level: high
          reason_code: policy_block
expect:
  status: sealed
  steps:
    - digest-source
    - detect
    - scrub
    - digest-redacted
    - finalize
  step_outputs:
    finalize:
      subset:
        redaction_report:
          data:
            decision: blocked
            redacted_digest: null
            residual_risk:
              level: high
              reason_code: policy_block
            scanner:
              status: block
        redacted_content: ""
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: redact-pii
  source_case: redact-pii-blocked
  source: skills-fixture
