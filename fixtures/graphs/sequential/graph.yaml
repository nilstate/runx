name: sequential-echo
owner: runx
steps:
  - id: first
    skill: ../../skills/json-output
    inputs:
      message: hello from graph
  - id: second
    skill: ../../skills/echo
    context:
      message: first.result.data.message
