skill: mcp-approval-graph
version: "0.1.0"
runners:
  default:
    default: true
    type: graph
    graph:
      name: mcp-approval-graph
      result_from:
        - approve
      steps:
        - id: approve
          run:
            type: approval
          inputs:
            gate_id: mcp-approval
            reason: Approve the MCP graph run.
          artifacts:
            wrap_as: approval
