skill: reviewer-boundary
version: "0.1.0"
runners:
  default:
    default: true
    type: agent-task
    agent: codex
    task: review-boundary
    outputs:
      verdict: string
    inputs:
      prompt:
        type: string
        required: true
